# Bumped fastapi from 0.115.0 to 0.115.6 in v1.21.4 — pulls in
# starlette >=0.41 which fixes CVE-2024-47874 (HIGH, multipart DoS).
fastapi==0.115.6
uvicorn[standard]==0.30.6
python-multipart==0.0.27   # required by FastAPI for multipart/form-data uploads (dict-import endpoint)

pydantic==2.9.2
pydantic-settings==2.5.2
# Pinned to the latest 1.x line; CVE-2026-28684 has no published fix
# yet, tracking upstream.
python-dotenv==1.0.1

sqlalchemy==2.0.35
alembic==1.13.3
psycopg[binary]==3.2.13

httpx==0.27.2
requests==2.32.3   # used by app/llm/groq_provider.py — was previously a transitive dep, now pinned
tenacity==9.0.0

rich==13.9.2
